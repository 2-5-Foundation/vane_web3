{
  "name": "wasm-minimal-test",
  "type": "module",
  "scripts": {
    "test": "bunx vitest",
    "test:run": "bunx vitest run",
    "start-relay": "bun run start-relay.ts",
    "test:with-relay": "bun run start-relay & sleep 5 && bunx vitest run; kill %1"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "@vitest/browser": "^3.2.4",
    "playwright": "1.55.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "viem": "^2.35.1"
  }
}
